name: Mis primeros pasos con Github Actions
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: "Lista archivos"
        run: |
          ls -lha
          pwd
          id

  deploy:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: "Lista archivos"
        run: |
          ls -lha
          pwd
          id